var globalVar = "glabal var, can outside the block";
let globalLet = "global variable, can be seen just in a scope";
const globalConst = "can not be reasigned, seen just in a scope, class";

//see it at page
console.log(globalVar);
console.log(globalLet);
console.log(globalConst);